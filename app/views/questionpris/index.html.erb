
<%
  content_for(:title, '任务优先级')
  content_for(:category_1, 'Tables')
%>
<% content_for :heading do %>
  <i class='fal fa-th-list text-primary'></i>  任务优先级
  <%= stylesheet_link_tag 'smartadmin/fa-solid', media: 'screen, print' %>
<% end %>


<div class="row">
  <div class="col-sm-12">
    <div class="card mb-g">

      <div class="card-body">
        <div class="frame-wrap bg-faded mb-5">
          <div class="frame-wrap p-0 border-0 m-0">
            <table class="table m-0 table-bordered table-hover" id="table-example">
              <thead>
              <tr>
                <th>优选级</th>
                <th>默认选择</th>
                <th style="width: 150px;">操作</th>
              </tr>
              </thead>
              <tbody>
              <% @questionpris.each do |questionpri| %>
                <tr>
                  <td><%= questionpri.pri %></td>
                  <td>
                    <% if questionpri.isdefault == 1 %>
                    <i class="fas fa-check text-primary"></i>
                      <% end %>
                  </td>
                  <td>
                    <%= link_to '编辑', edit_questionpri_path(questionpri), class:'btn btn-sm btn-primary' %>
                    <%= link_to '删除', questionpri_path(questionpri), method: :delete, class:'btn btn-sm btn-danger', data: { confirm: '确认删除?' } %>
                  </td>
                </tr>
              <% end %>
              </tbody>
            </table>

            <%= link_to '新增', new_questionpri_path, class:'btn btn-primary mt-3 float-right' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>